//Saida Digital
int vermelho = 13
int amarelo  = 12
int verde    = 11

//Entrada Digital
int botao = 3
int cartao = 6


void setup() {

//Luz Vermelha
	pinMode(vermelho, OUTPUT);
	pinMode(cartao || botao, INPUT);

  #luz Amarela
       pinMode(amarelo, OUTPUT);
       pinMode(cartao || botao, INPUT);

//Luz Verde
       pinMode(verde, OUTPUT);
       pinMode(cartao || botao, INPUT);

  
}

void loop() {
  
}
